{
  "name": "gfm-api",
  "version": "3.0.0",
  "description": "Global FDI Monitor API — 48 routes, PostgreSQL, Redis, WebSocket, Stripe",
  "main": "server.js",
  "scripts": {
    "start":     "node server.js",
    "dev":       "node --watch server.js",
    "test":      "node -e \"require('./server.js')\" & sleep 2 && curl -sf http://localhost:3001/api/v1/health && kill %1"
  },
  "dependencies": {
    "bcrypt":    "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "pg":        "^8.11.5",
    "redis":     "^4.6.14",
    "stripe":    "^14.25.0",
    "ws":        "^8.16.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "UNLICENSED"
}
